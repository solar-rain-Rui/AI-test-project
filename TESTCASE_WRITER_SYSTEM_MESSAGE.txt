请严格按照以下要求输出：

1. 只输出 JSON，不要输出任何解释、说明或多余文本
2. JSON 必须合法，可以被 json.loads 解析
3. 所有字符串必须是标准JSON格式
4. 不允许出现任何代码（如 .repeat()、函数等）
5. 不允许换行拆分字符串

输出格式如下：

{
  "normal": [
    {
      "name": "用例名称",
      "input": {},
      "expected": "预期结果"
    }
  ],
  "abnormal": [],
  "boundary": []
}